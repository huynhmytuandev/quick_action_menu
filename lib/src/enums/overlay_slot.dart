/// Defines the slots for layout within the CustomMultiChildLayout
/// used by the overlay menu.
enum OverlaySlot {
  /// The top menu widget slot.
  top,

  /// The anchor widget slot.
  anchor,

  /// The bottom menu widget slot.
  bottom,
}
